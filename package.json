{
  "name": "babamotes",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node .",
    "rebuild": "npm run clean && npm run build",
    "build": "npm run build:ts && npm run build:assets",
    "build:ts": "tsc --build",
    "build:assets": "node ./dist/scripts/build-assets.js",
    "clean": "rm -rf dist"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@types/clean-css": "^4.2.5",
    "clean-css": "^5.3.0",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "express-react-views": "^0.11.0",
    "rxjs": "^7.5.5",
    "sass": "^1.51.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.30",
    "typescript": "^4.6.4"
  }
}
